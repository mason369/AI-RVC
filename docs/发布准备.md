# 1.5.0 发布准备

当前源码候选版本为 `1.5.0-rc.1`，尚未发布；正式发行前将 `VERSION`、Compose 默认镜像标签及构建参数一并更新为确定版本。不能将未实测的平台列为已经通过。

## 本版范围

- audio-separator 0.47.0、Leap Instrumental 默认纯伴奏与 PolarFormer 移除；原有六模型、模型 YAML 参数及 Float32 WAV 输出保持。
- 同步多轨播放器、中英文、下载与上传修正；模型兼容性、配置保存及官方源码封装修正。
- Windows/Linux CPU 与 CUDA 便携构建入口，Linux x86-64 CPU/CUDA Docker 镜像、Compose、持久化、可选登录和健康检查。
- README、HF/Colab、平台与模型文档、真实截图及中英更新日志；完整模型兼容范围和仍未验证的环境单独记录。

详细改动见 [CHANGELOG](../CHANGELOG.md)，实测证据见[平台验收](平台适配与验收.md)。历史版本的测量和实现说明保留在历史章节，不能当作当前默认值。

## 当前验收进度

Docker CPU/CUDA 的本机构建、各 335 项全量回归、断网真实翻唱、网页上传与播放、原始 WAV 下载、登录、子路径代理、配置重建保留、镜像导出导入和数据备份恢复均已实测。启动配置固定设备时，界面和保存接口均明确限制修改。两个镜像是首次提供，没有上一版 Docker 镜像可对比；发行目录附完整文件清单及归档 SHA-256。

仍需用户试听候选版本；正式便携包需要按最终源码重建并逐包复验、对照旧版资产。远端 Actions、GHCR 发布、云端与未接入硬件没有计为完成，不能直接将早期便携包改名为本轮最终候选包。

## 发布前必须核对

1. 在最终源码上执行 `python -m unittest discover -s tests`，重新构建多轨资源，完整依赖 `pip check` 通过。
2. 用实际权重完成默认六模型翻唱，核验七类音频、参数、有限采样值与 Float WAV，并由用户亲自试听、检查页面；新缺陷未解决时停止发布。
3. 分别验收实际要分发的 Windows/Linux CPU/CUDA 包和 Docker 镜像；源码或设备探针通过不能替代运行包通过。缺少硬件的后端明确标为未验收或实验性。
4. 对照上一版包的运行依赖、上游源码、模型、配置、前端资源和入口清单解释增删。PolarFormer 不得残留在新发行资产中；角色权重不因本地存在就自动进入发行包。
5. 每个便携目录生成 `PACKAGE-MANIFEST.json`，压缩包与分卷生成对应 `SHA256SUMS`，下载后复核。README 引用的公开图片和文档一并打入便携包。
6. 核对 `VERSION`、Git 标签、源码提交、OCI 标签和文档指向同一版本；本机模型、录音、凭据、AGENTS.md 及私有记录不能提交或进入容器上下文。

## 工作流约定

`Build Executables` 手动运行默认只生成 Actions 审核产物。上传发行资产必须显式指定与 `VERSION` 一致的标签，并选择 `publish`；推送版本标签也要求目标 Release 已存在且仍为草稿。工作流检出该标签源码，拒绝把任意分支构建覆盖到“最新 Release”。四种包全部完成并复核之后，再发布草稿。

`Docker Images` 手动运行只构建和检查，不推送仓库；正式发布 Release 后才构建并推送 GHCR 的明确版本 CPU/CUDA 标签及 SBOM/来源证明。托管 runner 没有 NVIDIA GPU，只能验证 CUDA 镜像构建和依赖，真实 GPU 翻唱需使用本地验收证据。当前只声明 `linux/amd64`，没有把 ARM64 模拟构建当作真机验收。

本次任务完成发布准备，不自动 commit、push、打标签、覆盖现有 Release 或更新云端服务。用户确认候选版本之后再执行正式发布流程。
