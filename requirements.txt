# -*- coding: utf-8 -*-
# RVC AI 翻唱依赖

# PyTorch (根据 CUDA 版本选择)
torch>=2.3,<3; sys_platform != "darwin" or platform_machine != "arm64"
torch>=2.13,<3; sys_platform == "darwin" and platform_machine == "arm64"
torchvision>=0.15.0  # required transitively by audio-separator/onnx2torch; keep aligned with torch
torchaudio>=2.0.0; sys_platform != "darwin" or platform_machine != "arm64"
torchaudio>=2.11.0; sys_platform == "darwin" and platform_machine == "arm64"  # stable ABI supports torch>=2.11

# Gradio 界面
gradio==6.7.0
fastapi>=0.115,<1
anyio>=3,<5
pydantic>=2,<3
jinja2>=3.1,<4
pandas>=2,<3

# 音频处理
librosa>=0.10.0
soundfile>=0.12.0
av>=10.0.0
scipy>=1.10.0
numpy>=2,<3          # audio-separator 0.47.0 / Gradio 5 compatibility
PyYAML>=6.0
einops>=0.7.0
praat-parselmouth>=0.4.3
pyworld>=0.3.4
torchcrepe>=0.0.20
torchfcpe==0.0.4

# 向量检索
faiss-cpu>=1.7.4

# 工具库
tqdm>=4.65.0
requests>=2.28.0
gdown==6.0.0
python-dotenv>=1.0.0
colorama>=0.4.6

# MCP 服务器
mcp>=1.26,<2  # MCP 2.x requires Pydantic >=2.12, incompatible with Gradio 5.49.1
jsonschema>=4,<5

# AI 翻唱功能
demucs>=4.0.0          # 人声分离 (Demucs)
audio-separator==0.47.0        # install.py adds cpu/gpu/dml extra for the selected backend
huggingface_hub>=0.19.0,<1.0 # 模型下载；与 HF Space 运行环境保持稳定
pedalboard>=0.7.0      # 音频效果(混响)
imageio-ffmpeg>=0.5.1  # 便携 ffmpeg 运行时

ffmpeg-python>=0.2.0   # FFmpeg 绑定
fairseq==0.12.2        # 旧版 HuBERT 特征提取
transformers==5.10.0   # 固定新版官方 RVC 的 HuBERT 转换格式
